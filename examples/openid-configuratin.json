{
	"issuer": "https://<host>:<port>/api/v1/project/<project-name>",
	"authorization_endpoint": "https://<host>:<port>/api/v1/project/<project-name>/openid-connect/auth",
	"token_endpoint": "https://<host>:<port>/api/v1/project/<project-name>/optnid-connect/token",
	"userinfo_endpoint": "https://<host>:<port>/api/v1/project/<project-name>/optnid-connect/userinfo",
	"jwks_uri": "https://<host>:<port>/api/v1/project/<project-name>/optnid-connect/certs",
	"scopes_supported": [
		"openid"
	],
	"response_types_supported": [
		"code",
		"token",
		"id_token",
		"code token",
		"code id_token",
		"token id_token",
		"code token id_token",
		"none"
	],
	"subject_types_supported": [
		"public"
	],
	"id_token_signing_alg_values_supported": [
		"HS256"
	],
	"claims_supported": [
		"iss",
		"aud",
		"sub",
		"exp",
		"jti",
		"iat",
		"nbf"
	]
}